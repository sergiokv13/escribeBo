
        <h1 class="center-title">Recibidos</h1>

        <div class="table-responsive">
          <table class="table table-hover table-striped table-bordered">
            <thead>
              <tr>
                <th>Título</th>
                <th>Contenido</th>
                <th>Recibido de</th>
                <th>Fecha</th>
                <th>Visto</th>
                <th colspan="3"></th>
              </tr>
            </thead>

            <tbody>
              <% @inboxes.each do |inbox| %>
                <% if inbox.seen %>
                  <tr class="success">
                <% else %>
                  <tr class="danger">
                <% end %>
                  <td><%= inbox.subject %></td>
                  <td><%= raw(auto_link(inbox.content)) %></td>
                  <td><%= link_to inbox.user1.fullName, '/profile/' + inbox.user1.id.to_s %></td>
                  <td><%= inbox.created_at.to_date %></td>
                  <td><%= inbox.visto %></td>
                  <td><%= link_to 'Ver', inbox %></td>
                  <td><%= link_to 'Borrar', '/deleteInbox/' + inbox.id.to_s %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
        <div class="apple_pagination">
            <%= will_paginate @inboxes, :previous_label => 'Anterior', :next_label => 'Siguiente' %>
        </div>

        <h1 class="center-title">Enviados</h1>
        <div class="table-responsive">
          <table class="table table-hover table-striped table-bordered" >
            <thead>
              <tr>
                <th>Título</th>
                <th>Contenido</th>
                <th>Enviado a</th>
                <th>Fecha</th>
                <th>Visto</th>
                <th colspan="3"></th>
              </tr>
            </thead>

            <tbody>
              <% @enviados.each do |inbox| %>
                <% if inbox.seen %>
                  <tr class="success"></tr>
                <% else %>
                  <tr class="danger">
                <% end %>
                  <td><%= inbox.subject %></td>
                  <td><%= inbox.content %></td>
                  <td><%= link_to inbox.user2.fullName, '/profile/' + inbox.user2.id.to_s %></td>
                  <td><%= inbox.created_at.to_date %></td>
                  <td><%= inbox.visto %></td>
                  <td><%= link_to 'Ver', inbox %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
        <div class="apple_pagination">
            <%= will_paginate @enviados, :previous_label => 'Anterior', :next_label => 'Siguiente' %>
        </div>



