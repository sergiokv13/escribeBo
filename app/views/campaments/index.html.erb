
 <h1 class="text-center">Jurisdicción</h1>
  <div class="row">
   <div class="col-lg-4"></div>
    <div class="col-lg-4">
      <a class="image_link" href="/campaments/<%=@campaments.first.id%>">
        <div class="image_name"><%=@campaments.first.name%></div>
        <img class="img-responsive" src="<%=@campaments.first.image.url%>">
      </a>
    </div>
   
    <div class="col-lg-4"></div>
  </div>
   <h2 class="center-title">Departamentos</h2>
  <div class="row">

    <% @users = @campaments.where("id not in (?)", 1).map.each_with_index do |campament,i| %>

      <% if i%3 == 0 %>
        <div class="row">
          <div class="col-lg-12">
            <br>
          </div>

        </div>
      <% end %>
          <div class="col-lg-4 col-md-4 col-xs-4 col-sm-4">
            <a class="image_link" href="/campaments/<%=campament.id%>">
              <div class="image_name"><%=campament.name %></div>
              <img class="img-responsive" src="<%=campament.image.url%>">
            </a>
          </div>
    <% end %>

  </div>

