

<center><h1>Listado de Premios</h1></center>


<div class="row">
  <div class="col-lg-12">
    <a href="/premiacions/new" class="btn btn-success">Crear Premio</a>
    <br><br />
    <div class="table-responsive">
      <table class="table table-bordered table-hover table-striped">
        <thead>
          <tr>
            <th>Título</th>
            <th>Fecha</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @premiacions.each do |premiacion| %>
            <tr>
              <td colspan="2">
                <div class="dropdown config-bt llavecita">
                  <span id="dLabel" class="dropdown-toggle glyphicon glyphicon-wrench" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  </span>
                  <ul class="dropdown-menu" aria-labelledby="dLabel">
                    <% if current_user.is_oficial %>
                        <li><a href="/premiacions/<%= premiacion.id %>"> Ver</a></li>
                        <li><%= link_to "Editar", '/premiacions/' + premiacion.id.to_s + '/edit' %></li>
                    <% end %>
                  </ul>
                </div>
              </td>
              <td><%= premiacion.title %></td>
              <td><%= premiacion.date_of %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
