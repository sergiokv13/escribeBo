
    <h1 class="center-title">Cuerpos DeMolay</h1>
    <div class="row">
      <div class="col-lg-12 col-md-12 col-xs-12 col-sm-12">
        <h2 class="center-title">Capítulos</h2>
      </div>

      <% @capitulos.each_with_index do |chapter, index| %>

        <% if index%3==0 %>
          <div class="row">
            <div class="col-lg-12 col-md-12 col-xs-12 col-sm-12">
              <br>
            </div>
          </div>
        <% end %>

        <div class="col-lg-4 col-md-4 col-xs-4 col-sm-4">
          <a class="image_link" href="/chapters/<%=chapter.id%>" data-no-turbolink>
            <div class="image_name"><%=chapter.chapter_name%> #<%=chapter.number%></div>
            <img class="img-responsive" src="<%=chapter.image.url%>">
          </a>
        </div>
      <%end%>
    </div>

   <div class="row">
      <div class="col-lg-12 col-md-12 col-xs-12 col-sm-12">
        <h2 class="center-title">Prioratos</h2>
      </div>

      <% @prioratos.each_with_index do |chapter, index| %>

        <% if index%3==0 %>
          <div class="row">
            <div class="col-lg-12 col-md-12 col-xs-12 col-sm-12">
              <br>
            </div>
          </div>
        <% end %>

        <div class="col-lg-4 col-md-4 col-xs-4 col-sm-4">
          <a class="image_link" href="/chapters/<%=chapter.id%>" data-no-turbolink>
            <div class="image_name"><%=chapter.chapter_name%> #<%=chapter.number%></div>
            <img class="img-responsive" src="<%=chapter.image.url%>">
          </a>
        </div>
      <%end%>
    </div>


    <div class="row">
      <div class="col-lg-12 col-md-12 col-xs-12 col-sm-12">
        <h2 class="center-title">Cortes</h2>
      </div>

      <% @cortes.each_with_index do |chapter, index| %>

        <% if index%3==0 %>
          <div class="row">
            <div class="col-lg-12 col-md-12 col-xs-12 col-sm-12">
              <br>
            </div>
          </div>
        <% end %>

        <div class="col-lg-4 col-md-4 col-xs-4 col-sm-4">
          <a class="image_link" href="/chapters/<%=chapter.id%>" data-no-turbolink>
            <div class="image_name"><%=chapter.chapter_name%> #<%=chapter.number%></div>
            <img class="img-responsive" src="<%=chapter.image.url%>">
          </a>
        </div>
      <%end%>
    </div>
